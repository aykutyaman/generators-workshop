Example: Excpetion Delegation
=============================
In the same way that `yield`-delegation transparently passes messages through in both directions,
error/exceptions also pass in both directions.